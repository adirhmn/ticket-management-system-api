ENV=development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tickets
API_KEY=supersecretkey

# PostgreSQL specific variables for docker-compose.prod.yml
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tickets

# Comma-separated list of allowed CORS origins (default * allows all)
CORS_ORIGINS=*
